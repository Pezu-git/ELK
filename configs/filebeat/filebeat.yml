filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /usr/share/filebeat/nginx/access.log

    fields:
      service: nginx_logs

output.logstash:
  enabled: true
  hosts: ["logstash:5044"]        